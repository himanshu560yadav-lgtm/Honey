package com.blurr.voice.utilities

import android.content.Context

class WakeWordManager(private val context: Context) {

    fun isWakeWordEnabled(): Boolean = false

    fun enableWakeWord() {
        // Wake word disabled in this version
    }

    fun disableWakeWord() {
        // Wake word disabled in this version
    }

    fun startWakeWordListener() {
        // Wake word disabled in this version
    }

    fun stopWakeWordListener() {
        // Wake word disabled in this version
    }
}